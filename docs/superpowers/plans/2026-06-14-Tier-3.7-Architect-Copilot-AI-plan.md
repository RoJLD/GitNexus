# Tier 3.7 — Architect's Copilot AI — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan phase-by-phase.

**Goal:** Livrer un agent LLM sidecar qui consomme la surface MCP analytics existante et produit un rapport narratif structuré (Constat → Causes → Reco → Confiance) à 3 profondeurs, accessible via UI panel + REST SSE + MCP tool + skill Claude.

**Spec source:** [`docs/superpowers/specs/2026-06-14-Tier-3.7-Architect-Copilot-AI.md`](../specs/2026-06-14-Tier-3.7-Architect-Copilot-AI.md)

**Working directory:** `c:\Users\robla\VScode_Project\ELYSIUM\ingestion\GitNexus`

**Estimation totale** : **~10-15 jours** (MVP 3-5j + Polish 5-7j + Hardening 2-3j)

---

## Phase A — MVP (3-5 jours)

> Objectif : un tour quick + standard fonctionne end-to-end UI → worker → MCP → LLM → SSE retour, sans cache ni mode deep.

### Task A1 — Inventory gate MCP analytics (0.5j)
- [ ] Valider `node mcp-server/smoke.mjs` passe 6/6 contre stack live
- [ ] Mapper les 9 endpoints requis aux 12 MCP tools existants ; documenter dans plan
- [ ] Si gap → bloquer, sinon greenlight Phase A

### Task A2 — `tour-worker.mjs` skeleton (1j)
- [ ] Créer `tour-worker.mjs` à la **repo ROOT** (cohérent avec `wiki-worker.mjs` Tier 2.5)
- [ ] HTTP :4749, endpoint `POST /tour` stub retournant SSE 4 sections markdown hardcoded
- [ ] Test unitaire pure fn `buildTourPrompt(depth)` dans `tests/unit/tour-prompt.test.mjs`

### Task A3 — REST endpoint `POST /tour` stub (0.5j)
- [ ] `upstream/docker-server-tour.mjs` proxy SSE 1:1 vers `http://gitnexus:4749/tour`
- [ ] `docker-server.mjs` MOD monte `handleTourRoute`
- [ ] Smoke `curl -N -X POST http://localhost:4747/tour` reçoit 4 events stub

### Task A4 — MCP client embedded dans worker (1j)
- [ ] Spawn `mcp-server/server.mjs` en sibling process depuis `tour-worker.mjs`
- [ ] JSON-RPC 2.0 client minimaliste (stdio)
- [ ] Smoke : worker invoque `gitnexus_entropy` + `gitnexus_ownership` et log les résultats

### Task A5 — Prompt template `quick` + LLM call (1j)
- [ ] Template 4 sections en `gitnexus-web/src/lib/tour-prompt.ts` (testable)
- [ ] LLM call via `GITNEXUS_LLM_BASE_URL` (OpenAI-compat), structured output JSON
- [ ] Tour `quick` end-to-end : real repo → 4 sections markdown < 30s

### Task A6 — `TourPanel.tsx` basic UI (1j)
- [ ] Composant React + `useTour({ repo, depth })` hook EventSource
- [ ] Rendu sections incrémentales avec marked.js
- [ ] Sélecteur depth (quick/standard/deep) + bouton "Generate tour"
- [ ] E2E Playwright : ouvrir panel, déclencher tour quick, voir 4 sections

**Sortie Phase A** : tour `quick` UI → SSE → LLM → markdown fonctionnel. Tour `standard`/`deep` retournent stubs. Pas de cache.

---

## Phase B — Polish (5-7 jours)

> Objectif : couvrir les 3 profondeurs, exposer MCP tool `gitnexus_tour`, ajouter cache, skill Claude.

### Task B1 — Profondeurs `standard` + `deep` (1.5j)
- [ ] Templates étendus consommant les 9 endpoints MCP
- [ ] Logique adaptative : `deep` invoque tous les tools en parallèle (Promise.all), agrège
- [ ] Test : `depth: deep` produit >2KB markdown, `toolsCalls` ≥ 9 dans event done

### Task B2 — Cache TTL par `(repo, depth, snapshotId)` (1j)
- [ ] LRU in-memory dans `tour-worker.mjs`, TTL 300s pour HEAD, ∞ pour snapshots passés
- [ ] Header `X-Cache: HIT|MISS` + event `done { cacheHit }`
- [ ] Test : 2 appels identiques → 2ème < 100ms

### Task B3 — MCP tool `gitnexus_tour` (0.5j)
- [ ] Ajout dans `mcp-server/server.mjs` registre tools
- [ ] Wrappe `POST /tour` en mode non-streamé (attendre `event: done` puis retourner JSON complet)
- [ ] Smoke `node mcp-server/smoke.mjs --tool=tour` retourne rapport complet

### Task B4 — Skill Claude `.claude/skills/gitnexus-architect.md` (0.5j)
- [ ] Description + usage examples + invocation pattern
- [ ] Cross-link vers MCP tool `gitnexus_tour`
- [ ] Test : Claude Code reconnaît le skill, l'invoque sur repo réel

### Task B5 — Confidence scoring per section (1j)
- [ ] Pure fn `computeConfidence(section, sources)` dans `tour-prompt.ts`
- [ ] Heuristiques : sample size, snapshot age, completeness des tools calls
- [ ] Test unit : 8 cas (high/medium/low confidence sur sections variées)

### Task B6 — Mode dégradé + error handling (1j)
- [ ] `GITNEXUS_API_KEY` absent → 503 + message UI explicite
- [ ] LLM timeout → SSE `event: error { recoverable: true }` + retry button UI
- [ ] Test : disable LLM env → endpoint répond 503 propre, UI affiche fallback

### Task B7 — Integration Mycelium synergy (1.5j)
- [ ] Append-only audit log `data/governance/architect_tour_audit.jsonl` (hash-chained)
- [ ] Optionnel : consommer Belt Market BLT ledger pour annoter coût économique reco
- [ ] Optionnel : citer sortance meta_score + merge_survival depuis SIGIL-1255/1252
- [ ] Test : tour produit log entry, hash chain vérifiable

**Sortie Phase B** : 3 profondeurs fonctionnelles, cache, MCP tool, skill Claude, confidence, mode dégradé, audit hash-chained. Feature-complète selon spec.

---

## Phase C — UI Polish (LIVRÉ 2026-06-15 commit `c835ad25`)

> Sortie : `CopilotPanel.tsx` 4 sub-panels x 4 states + polling 30s/60s + memo + debounce + chain cache 5min + 16 vitest verts. Iron Rules COPILOT-UI-1..3 gravées.

---

## Phase D — Hardening (SCAFFOLDED 2026-06-15, sortie sur `feat/tier37-phase-d-hardening`)

> Objectif : tests E2E complets, perf instrumentation Prometheus, INVENTORY+ROADMAP update, Iron Rules COPILOT-HARDENING-1..3 gravées.

### Task D1 — `/copilot/metrics` Prometheus endpoint (~150 LOC) — LIVRÉ
- [x] `upstream/docker-server-copilot-metrics.mjs` (240 LOC, additif via `patches/additive-files.diff` +240 lignes)
- [x] Counter `copilot_endpoint_calls_total{endpoint,status}`
- [x] Histogram `copilot_endpoint_duration_seconds{endpoint}` (buckets 10ms..10s)
- [x] Counter `copilot_chain_verify_count_total{result=valid|invalid}` (labels stables même à zéro)
- [x] Counter `copilot_cache_hit_total{type=aggregate|miss}` (labels stables même à zéro)
- [x] `handleCopilotMetricsRoute(req,url,res)` retourne text/plain v0.0.4
- [x] Helpers exportés : `instrumentEndpoint` / `incrementChainVerify` / `incrementCacheHit` / `renderPrometheusText` / `resetRegistryForTests`
- [ ] WIRING follow-up : importer dans `docker-server-routes.mjs` + appeler `instrumentEndpoint` dans chaque sub-handler copilot (différé Phase D.2 — scaffold suffit pour scrape Prometheus)

### Task D2 — E2E Playwright `copilot-panel.spec.ts` (~200 LOC) — LIVRÉ
- [x] `tests/e2e/specs/copilot-panel.spec.ts` (8 scenarios) :
  - Panel renders 4 sub-panels (inventory, BLT, cluster, forge)
  - Refresh button triggers loading state + fires `/copilot/mcp-inventory`
  - Mocked inventory response displayed
  - Fetch error → `copilot-error-toast` visible (jamais crash)
  - Filter tabs switching (`data-active=true`)
  - `X-Elysium-Cache: hit` header surfaces dans `copilot-cache-badge`
  - `/copilot/metrics` retourne Prometheus v0.0.4 avec 4 familles
  - Counter monotonic après call copilot
- [x] Réutilise convention `tests/e2e/specs/wiki-panel.spec.ts` (BASE_URL env, data-testid first)
- [x] `tests/package.json > devDependencies > @playwright/test ^1.49.0` déjà présent (Phase 1 test pyramid)
- [x] `tests/package.json > scripts > test:e2e: playwright test --config e2e/playwright.config.ts` déjà présent

### Task D3 — INVENTORY.md count update — LIVRÉ
- [x] `patches/additive-files.diff` ligne 127 : `~99` → `~138` (+39 : Phase A 5 copilot + Phase C 1 panel + Phase D 1 metrics + accumulation 32 autres fichiers depuis 2026-05-29)
- [x] Snapshot date : `2026-06-14` → `2026-06-15 (post-Tier-3.7 Phase D Hardening scaffold)`
- [x] Section Tier 3.7 : `⏳` → `🟢 Phase A+B+C livré, Phase D Hardening scaffolded 2026-06-15` avec liste consumers complète

### Task D4 — Iron Rules COPILOT-HARDENING-1..3 gravées — LIVRÉ
- [x] COPILOT-HARDENING-1 Σ-METRICS-PROMETHEUS-MANDATORY (spec § 7)
- [x] COPILOT-HARDENING-2 Σ-E2E-PLAYWRIGHT-COVERAGE (spec § 7)
- [x] COPILOT-HARDENING-3 Σ-INVENTORY-COUNT-MANDATORY (spec § 7)

### Task D5 — REMAINING follow-ups (différés Phase D.2)
- [ ] Wire `instrumentEndpoint()` calls dans les 5 sub-handlers `docker-server-copilot{,-blt,-cluster,-forge,-core}.mjs` (consumer du registry — scaffold seul ne bump pas les counters)
- [ ] `docker compose up` + scrape live `/copilot/metrics` confirme 200 + body Prometheus
- [ ] `playwright test --config e2e/playwright.config.ts copilot-panel.spec.ts` contre stack live (8/8 GREEN attendu, certains tests gated sur sub-panel testids qui peuvent encore être en cours d'implémentation Phase C)
- [ ] Wire `gitnexus_copilot_metrics` MCP tool (optionnel — exposition `/copilot/metrics` via stdio JSON-RPC pour scrape agent-side)
- [ ] CI : tour smoke après `docker compose build` (Phase 1 test pyramid follow-up)

**Sortie Phase D** : Tier 3.7 Hardening SCAFFOLDED (metrics + E2E + INVENTORY + 3 Iron Rules) ; LIVRÉ prod-ready après D5 wiring + live validation.

---

## Dependencies critiques

- **Tier 2bis.1 MCP exposure** ✅ LIVRÉ — bloquant, vérifié à A1
- **Tier 2.5 Code Wiki worker pattern** ✅ LIVRÉ — réutilisation directe pour A2 (Dockerfile.cli wrapper, env LLM partagé)
- **`GITNEXUS_API_KEY` env** — config opérateur, documenté dans Phase C3

## Risks & mitigations

| Risk | Mitigation |
|---|---|
| Coût LLM par tour (deep ~$0.10-0.50) | Cache by snapshotId ∞ + bouton manuel, jamais auto-cron en V1 |
| Tour deep > 60s timeout | SSE streaming + parallel tool calls + p95 < 45s budget |
| Hallucination LLM sur métriques | Structured output JSON forcé + sources citées par section (event includes `sources: [tool_call_id]`) |
| Concurrence Akon Labs Chemin A | Positionnement Chemin B (Copilot, pas CI bloquante) — différenciation messaging |

## Total estimate

**~10-15 jours développeur** (Phase A 3-5j + Phase B 5-7j + Phase C 2-3j) pour Tier 3.7 prod-ready.
